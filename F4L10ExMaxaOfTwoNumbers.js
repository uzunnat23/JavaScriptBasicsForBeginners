// Write a function that takes two numbers and returns the maximum of the two.

let number = maxNum(2, 2);
console.log(number);
function maxNum(firstNumber, secondNumber) {
    return (firstNumber > secondNumber) ? firstNumber : secondNumber;
}

